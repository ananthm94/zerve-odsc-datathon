"""Shared infrastructure used across features: config, contracts, LLM access,
SQL validation, query runners, and retrieval. Modules here must not import from
feature packages (ask/library/dashboard/experiments/ui)."""
