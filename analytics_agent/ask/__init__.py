"""The "Ask" tab: the autonomous LangGraph trust loop (route -> ground in the
semantic layer -> plan -> query -> reflect -> synthesize -> verify) plus its
supporting pieces (semantic layer, model router, snapshot verification)."""
